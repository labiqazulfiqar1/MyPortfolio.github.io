name: Unzip
on: push
jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: unzip PortWebsite.zip -d ./
      - run: rm PortWebsite.zip
      - uses: stefanzweifel/git-auto-commit-action@v4
